const { default: Link } = require("next/link")

const BlogCard = () => {

    return (
        <div>
            <Link href="#">
                <div>
                    
                </div>
            </Link>
        </div>
    )
}